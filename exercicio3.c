#include <stdio.h>

int main(void) {
    char str[100];
    char strDoPadrao[3];

    //pegando a entrada do usuario
    printf("Digite um numero: ");
    scanf("%99s", str);

    //identificando o padrao que a entrada segue
    for (int i=0; i < strlen(str); i++) {
        
    }
}